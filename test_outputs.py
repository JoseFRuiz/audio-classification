# Check the outputs of the model
# We can see the output norm.
# Check the weights norm
# Check the gradients norm
# An alternative to binary cross entropy loss could be using a loss that computes the maximum instead of the mean

